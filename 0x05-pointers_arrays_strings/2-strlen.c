#include "main.h"
/**
 * _strlen - function that check the lenght of a string and return it
 * @s: declaration of *s and paramters for the function _strlen
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
